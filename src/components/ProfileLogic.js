import React, { Component } from 'react';

class ProfileLogic extends Component {

  render() {
    return (
      <div id="content">
        <h1>User Profile</h1>
       
          <button type="submit" className="btn btn-primary">Log out</button>
       
      </div>
    );
  }
}

export default ProfileLogic;